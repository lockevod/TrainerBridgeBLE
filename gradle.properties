org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Build MUST run on JDK 17 (AGP 8.5 rejects JDK 21/23/26). Android Studio's bundled JDK is 17.
# From CLI: export JAVA_HOME=$(/usr/libexec/java_home -v 17)
